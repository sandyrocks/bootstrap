<!DOCTYPE html>
<html>
<head>
  <title>Bootstrap</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<% if current_user %>
<div class="navbar navbar-inverse">
        <div class="container">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand active" href="#">Be In Touch</a>
          </div>
          <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
              <li class="active"><a href="#">Home</a></li>
              <li class="active"><%= link_to 'Post', new_post_path %></li>
              <li class="active"><%= link_to 'Category', categories_path %></li>
              <li class="active"><%= link_to 'Blog', blog_path %></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
              <% if current_user %>
                <li class="active"><a href="#">Welcome <%= get_welcome_name %></a></li>
                <li class="active"><%= link_to 'Logout', destroy_user_session_path, :method => :delete  %></li>
                <li class="active"><%= link_to 'Profile', new_profile_path %></li>
              <% end  %>
          </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
  <% end %>
<body>

<%= yield %>

</body>
</html>
